#include<iostream>
#include <chrono>
using namespace std::chrono;

//chrono是一个模板库，使用简单，功能强大，只需要理解三个概念：duration、time_point、clock

void test1() {
	//td::chrono::duration 表示一段时间，比如两个小时，12.88秒，半个时辰，一炷香的时间等等，只要能换算成秒即可。

	//1 template <class Rep, class Period = ratio<1> > class duration;
}

int main() {
	test1();
}